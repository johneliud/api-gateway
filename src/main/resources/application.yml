server:
  port: 8083

spring:
  application:
    name: api-gateway
  config:
    import: optional:classpath:application-secrets.properties
  cloud:
    gateway:
      default-filters:
        - RemoveRequestHeader=X-User-Id
        - RemoveRequestHeader=X-User-Role

logging:
  level:
    io.github.johneliud.api_gateway: INFO
